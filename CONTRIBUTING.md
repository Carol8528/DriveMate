# 贡献指南

感谢参与 DriveMate。提交改动前请：

1. 从独立分支提交单一、可解释的改动；不得提交 `.env`、密钥、个人数据、运行数据库或客户资料。
2. 新增工具时同时提交 JSON Schema、执行实现、风险等级、确认规则与测试。
3. 新增知识内容时注明来源边界；法规、车型参数和安全结论不得凭空编写。
4. 运行 `python -m unittest discover -s tests -t .` 以及 README 中的验证脚本。
5. 在变更说明中写明动机、测试结果、已知限制；删除被新实现替代的冲突代码和文档。

提交贡献即表示你有权按本项目许可证提供相关内容。
