# 本地知识库

本目录是可随仓库检查的 Markdown 知识库，由 `components/knowledge_retriever.py` 按关键词检索并在响应的 `knowledge_refs` 中返回来源。内容用于演示检索、引用和安全边界，不替代具体车型随车手册、实时法规数据库或专业意见。

每份文件均注明适用边界。涉及车型参数、故障码、法规条文和处罚标准时，系统必须提示用户以车辆厂商或主管机关的当前正式文本为准。
